Now we're adding a Deployment. This will take our image built in the previous branch, and deploy it to OpenShift.

To apply:
```
oc apply -f deployment.yml
```

You can see it running in your OpenShift project now.
